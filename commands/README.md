# Commands

Photography exposes two main command roots:

```text
/photography
/video
```

Both require permission level 2.

`/photography` primarily manages media items and reloads files.

`/video` controls fullscreen cutscenes.

See [Full Command Reference](reference.md) for every public command shape.
